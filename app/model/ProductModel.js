Ext.define('PracticeSencha.model.ProductModel', {
    extend: 'Ext.data.Model',
    alias: 'model.productModel',

    config: {
        fields: [
            {
                name: 'name'
            }
        ]
    }
});